## 1. Store layer: detect and preserve corrupted data

- [x] 1.1 In `local-storage-store.ts`, factor out a parse check that treats both unparseable JSON and valid-JSON-but-not-an-array as "corrupted" (matching the existing `Array.isArray` guard), reused by both `readAll()` (returns `[]` on corruption, used by `get`/`save`/`remove`) and the new `list()` logic below. Verify with a temporary script covering: a valid array, non-JSON garbage, and valid JSON that isn't an array — all three must be treated as corrupted except the first. (Implemented as `parseNotes()` + `readRaw()`, shared by `readAll()` and `list()`. Verified in the running app via dynamic `import()` of the module: valid array → resolves; non-JSON garbage → throws; valid-JSON-non-array → throws — matching expectations exactly.)
- [x] 1.2 Add a backup helper that copies the raw stored string verbatim to a separate key (e.g. `memo.notes.v1.corrupted-backup`), wrapped in its own `try`/`catch` so a failed backup write never blocks reporting the corruption itself. Verify by seeding a corrupted string and confirming the backup key holds it afterward. (Verified: seeded `'garbage-for-backup-test {{{'`, called `list()`, confirmed the backup key held the exact same string afterward.)
- [x] 1.3 Change `list()` so that when the stored data is corrupted, it calls the backup helper and throws a descriptive `Error` (Korean message per design.md, e.g. `메모를 불러오지 못했어요. 저장된 데이터에 문제가 있는 것 같아요.`) instead of returning `[]`. `get`, `save`, and `remove` are unaffected and keep resolving normally (treating corrupted data as empty via `readAll()`). Verify: with corrupted data seeded, `list()` rejects while `get()`/`save()`/`remove()` all still resolve. (Verified with corrupted data seeded: `list()` rejected with the message above; `get('nonexistent')` resolved to `undefined`; `save(...)` resolved with the saved note; `remove('nonexistent')` resolved — none of the three threw.)
- [x] 1.4 Verify the self-healing path end-to-end: seed corrupted data, call `save()` with a new note, confirm it succeeds, the primary key now holds valid JSON containing just that note, the backup key still holds the *original* corrupted string untouched, and a subsequent `list()` call now resolves normally instead of throwing. (Verified: first `list()` threw, `save()` then succeeded, primary key held only the new note as valid JSON, backup key still held the original corrupted bytes unchanged, and the second `list()` resolved normally with the new note.)

## 2. Wire the load failure into app state

- [x] 2.1 In `App.svelte`, wrap the `onMount` call to `noteStore.list()` in try/catch: on success, behave as today; on failure, set `notes = []` and a new `loadErrorMessage: string | null` state to the caught error's message (mirroring how `persistDraft()` already derives `saveErrorMessage`). Verify `pnpm run check` passes with the new state added.
- [x] 2.2 Pass `loadErrorMessage` down as a new prop to `NoteListPanel`.

## 3. Show the load failure in the list panel

- [x] 3.1 Add a `loadErrorMessage: string | null` prop to `NoteListPanel.svelte`. When present, render it in place of the ordinary empty-state message, styled with `color-danger` (matching the existing save-status error treatment in `NoteEditPanel.svelte`), while the header, "새 메모" button, and search field all remain exactly as they are today. Verify visually: with `loadErrorMessage` unset, the panel is pixel-identical to before this change; with it set, the error message replaces only the empty-state area. (Verified via screenshot with corrupted `localStorage` seeded: header/새 메모 button/search field unchanged, error text shown in `color-danger` red in the empty-state area, `role="alert"` set.)
- [x] 3.2 Verify the panel still shows the ordinary "아직 메모가 없어요" message (not the load-error one) when `notes` is empty and `loadErrorMessage` is `null` — the two states must stay visually distinct and independently triggerable. (Verified: with `localStorage` genuinely empty, page text showed the ordinary "아직 메모가 없어요..." message, not the load-error message.)

## 4. Verification

- [x] 4.1 Manually walk through every scenario in `specs/note-taking/spec.md`'s "Load failure is shown, not hidden" requirement in the running dev app: seed a corrupted `localStorage` value directly, reload, and confirm (a) the distinct load-failure message appears instead of the empty-state message, (b) creating a new note still works normally, (c) after that save, reloading again shows the note normally with no error message, (d) the backup key retains the original corrupted string throughout. (All four verified in the running app: screenshot confirmed the red error message with header/button/search intact; created a note titled "복구 테스트 메모" through the actual UI while in the corrupted state, which succeeded; reloaded and confirmed the note now shows normally with no error message; confirmed via `localStorage` that the backup key still held the original `'{{{ not valid json'` string unchanged.)
- [x] 4.2 Run `pnpm run check` and fix any type errors introduced by the above.

## 5. Show the preserved raw data in the edit panel (added after initial implementation, per user request)

- [x] 5.1 Export a standalone `getCorruptedBackup(): string | null` from `local-storage-store.ts` reading the backup key, wrapped in its own `try`/`catch`. Not added to the `NoteStore` interface — see design.md's decision on why this is storage-specific, not a general store capability.
- [x] 5.2 In `App.svelte`, add a `loadErrorRawData: string | null` state set from `getCorruptedBackup()` alongside `loadErrorMessage` in the `onMount` catch block, and pass it down to `NoteEditPanel` as a new prop.
- [x] 5.3 In `NoteEditPanel.svelte`, when `draft === null` and `loadErrorRawData` is present, replace the "select a note or start a new one" placeholder with the raw text (in a scrollable, monospace, selectable block) and a "복사" button using `navigator.clipboard.writeText`, silently falling back to leaving the text selectable if clipboard access is denied. Verify visually: with no load error, the panel is unchanged; with one, the raw corrupted string appears with a working copy button. (Verified via screenshot with corrupted data seeded: raw text block and 복사 button render correctly. Clipboard write itself could not be confirmed in this automation environment — `navigator.permissions.query({name: 'clipboard-write'})` reports `denied` there — but the `try`/`catch` handled the failure silently with no console error, and the text remained selectable, matching the designed fallback; this should work normally in a real user's browser with clipboard permission granted.)
- [x] 5.4 Run `pnpm run check` and confirm no type errors from the new prop/state.
